// Vamos a crear un generador de historias aleatorias
// para poner en práctica lo aprendido en la lección 1

// Aún no hemos hablado de las funciones, pero las usaremos 
// para hacer este juego. 
// la palabra "function" es una palabra reservada, se usa cuando
// queremos crear una función, después de la palabra reservada,
// tenemos que ponerle un nombre a la función, en este caso "generarHistoria"

// Las funciones pueden tener parámetros (lo que va dentro de () ) 
// pero también pueden no tener, como en este caso. Después se colocan {}
// que es lo que limita el cuerpo de la función, todo lo que va dentro
// de las llaves, forma parte de la función

function generarHistoria() {

}

// Dentro de la función crearemos un array llamado sujetos

function generarHistoria() {

    const sujetos = ['El perro', 'Mi abuela', 'Un payaso', 'El detective']

}

// Se trata de una array que contiene Strings

// Ahora crearemos 3 arrays más llamados "verbos, objetos, lugares"


function generarHistoria() {

    const sujetos = ['El perro', 'Mi abuela', 'Un payaso', 'El detective']
    const verbos = ['compró', 'saltó', 'cocinó', 'resolvió']
    const objetos = ['una bicicleta', 'en un charco', 'una deliciosa cena', 'el misterio']
    const lugares = ['en el mercado', 'en el parque', 'en la feria', 'en la escena del crimen']

}

// Para que la frase sea aleatoria, crearemos un índice random. 